spring:

  application:
    name: FinalProject

  datasource:
    url: jdbc:postgresql://dpg-cum7gm56l47c73961mr0-a.frankfurt-postgres.render.com:5432/ishonch_pdp
    username: ishonch_pdp_user
    password: SpDFlT4olnuhYVURUr3i6wRpsz6m9CG4
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /ui

server:
  port: 8080

