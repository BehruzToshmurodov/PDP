spring:

  application:
    name: FinalProject

  datasource:
    url: jdbc:postgresql://dpg-cu1rkr8gph6c73ekru4g-a:5432/pdp_db
    username: pdp_db_user
    password: q5kTNq4i6P3JvGrC77gvTpjlHOV7JITf
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /ui

server:
  port: 8080
